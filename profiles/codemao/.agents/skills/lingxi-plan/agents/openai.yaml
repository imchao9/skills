interface:
  display_name: "灵犀·需求方案"
  short_description: "读取并校准多来源需求，生成可实施的前后端技术方案"
  default_prompt: "使用 $lingxi-plan 读取并校准钉钉、本地或对话需求，生成需求清单和可实施的前后端技术方案；来源缺口必须明确标记。"

policy:
  allow_implicit_invocation: false
